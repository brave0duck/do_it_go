5장 연산자

- go에서는 c/c++처럼 ++a 전위연산 또는 b = a++같은 식을 쓸수없다
- int형 변수를 문자열로 변환하고싶으면 string(20) 이렇게는 불가능하다. 이 변환은 숫자20이 아닌
유니코드 제어 문자코드를 반환하게된다.
- 문자열 -> 숫자 변환 import "strconv" 후에 strconv.Atoi(a) 
- 숫자 -> 문자 변환은 strconv.Itoa(20) 이런식으로 써야한다


